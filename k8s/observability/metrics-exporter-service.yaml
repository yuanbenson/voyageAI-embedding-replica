apiVersion: v1
kind: Service
metadata:
  name: embedding-metrics-exporter
  namespace: inference
spec:
  type: ClusterIP
  selector:
    app: embedding-metrics-exporter
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
