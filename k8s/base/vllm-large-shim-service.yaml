apiVersion: v1
kind: Service
metadata:
  name: vllm-large-shim
  namespace: inference
spec:
  type: ClusterIP
  selector:
    app: vllm-large-shim
  ports:
    - name: http
      port: 8000
      targetPort: http
