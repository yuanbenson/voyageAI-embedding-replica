apiVersion: v1
kind: Service
metadata:
  name: embedding-api
  namespace: inference
spec:
  type: ClusterIP
  selector:
    app: embedding-api
  ports:
    - name: http
      port: 80
      targetPort: http
